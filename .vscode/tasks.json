{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run server",
      "type": "shell",
      "command": "cargo run",
      "options": {
        "env": {
          "MONGODB_URI": "mongodb://mongo:27017",
          "PORT": "3000"
        }
      },
      "problemMatcher": "$rustc"
    }
  ]
}
